services:
  - type: web
    name: streamaura-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn main:app --host 0.0.0.0 --port $PORT
    rootContext: backend
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
